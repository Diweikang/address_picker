const host = 'http://oms.magpiee.com.cn:28080'

const config = {
  host,
  searchCity: `${host}/api/v1/product/areas/search`,
  searchAreasGroup: `${host}/api/v1/product/areas/search/group`
}
export default config
