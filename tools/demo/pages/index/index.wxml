<view class="address-picker">
	<areas-group bind:change="onChange" groups="{{groups}}" />
	<location/>
	<hot-citys hotCitys="{{hotCitys}}" />
	<city-columns bind:select="getColumnValue" cityColumns="{{cityColumns}}" />
</view>

